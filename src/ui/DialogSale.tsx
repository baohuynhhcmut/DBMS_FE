import { Description, Dialog, DialogPanel, DialogTitle } from '@headlessui/react'


type Props = {
    open: boolean;
    handleClose: () => void;
}

const DialogSale = ({ open,handleClose }:Props) => {
  return (
    <>      
    
    </>
  )
}

export default DialogSale